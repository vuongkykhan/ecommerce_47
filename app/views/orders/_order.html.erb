<tr>
  <td><%= order.created_at %></td>
  <td><%= order.id %></td>
  <td><%= t ".status" %></td>
  <td><%= t ".total_price" %></td>
  <td><%= link_to t(".details"), order_order_items_path(current_user, order), class: "btn btn-primary" %></td>
</tr>
