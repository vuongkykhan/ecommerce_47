<%= provide :title, t(".profile") %>

<div class="col-md-8">
  <div class="well well-sm">
    <div class="row">
      <div class="col-md-4">
        <%= gravatar_for @user, size: Settings.gravatar_profile.size %>
      </div>
      <div class="col-md-8">
          <h4><%= @user.name.upcase %></h4>
          <p>
            <i class="glyphicon glyphicon-earphone"></i> <%= @user.phone %>
            <br>
            <i class="glyphicon glyphicon-home"></i> <%= @user.address %>
            <br>
            <i class="glyphicon glyphicon-envelope"></i> <%= @user.email %>
            <br>
            <i class="glyphicon glyphicon-road"></i> <%= @user.city %>
            <br>
            <i class="glyphicon glyphicon-globe"></i> <%= @user.country %>
          </p>
          <%= link_to t(".edit_profile"), edit_user_path(@user), class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</div>
