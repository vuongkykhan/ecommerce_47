<tr>
  <td><%= order.created_at %></td>
  <td><%= order.id %></td>
  <td><%= t "order.status.#{order.status}" %></td>
  <td><%= t ".currency", price: order.total_price %></td>
  <td>
    <%= link_to t(".view"), admin_order_order_items_path(order), class: "btn btn-primary" %>
  </td>
</tr>
